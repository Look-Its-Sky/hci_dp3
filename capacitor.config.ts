import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dp3prototype',
  appName: 'dp3_prototype',
  webDir: 'build'
};

export default config;
